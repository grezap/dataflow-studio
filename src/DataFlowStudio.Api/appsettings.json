{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "//": "OltpDb connection is injected at deploy time by the Vault Agent (nexus/dataflow-studio/oltpdb). This placeholder is overridden per environment; never commit real credentials."
  }
}
