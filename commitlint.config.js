/**
 * Conventional Commits (MASTER-PLAN E19). Activated via the Husky commit-msg hook after
 * `npm install`. Types: feat, fix, chore, docs, test, refactor, perf, build, ci, style, revert.
 */
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
