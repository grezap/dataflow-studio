<Project>

  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>

  <ItemGroup Label="nexus-shared (0.J) — the Nexus.* family from GitHub Packages">
    <PackageVersion Include="Nexus.Primitives" Version="0.1.0" />
    <PackageVersion Include="Nexus.Vault" Version="0.1.0" />
    <PackageVersion Include="Nexus.Kafka" Version="0.1.0" />
    <PackageVersion Include="Nexus.Avro" Version="0.1.0" />
    <PackageVersion Include="Nexus.Observability" Version="0.1.0" />
  </ItemGroup>

  <ItemGroup Label="Data access — Dapper on the AOT-friendly paths (E4: no EF Core on AOT paths)">
    <PackageVersion Include="Dapper" Version="2.1.66" />
    <PackageVersion Include="Microsoft.Data.SqlClient" Version="6.0.2" />
  </ItemGroup>

  <ItemGroup Label="ASP.NET Core host — OpenAPI (E14) generated from the composition root">
    <PackageVersion Include="Microsoft.AspNetCore.OpenApi" Version="10.0.9" />
    <!-- Transitive pin: AspNetCore.OpenApi 10.x resolves Microsoft.OpenApi 2.0.0, which carries
         advisory GHSA-v5pm-xwqc-g5wc (patched in 2.7.5). Pin up to the latest 2.x. -->
    <PackageVersion Include="Microsoft.OpenApi" Version="2.10.0" />
  </ItemGroup>

  <ItemGroup Label="Migrations — FluentMigrator for OltpDb (E1: up -> down -> up gate)">
    <PackageVersion Include="FluentMigrator" Version="6.2.0" />
    <PackageVersion Include="FluentMigrator.Runner" Version="6.2.0" />
  </ItemGroup>

  <ItemGroup Label="Migrations — DbUp (SQL-script, forward-only) for the StarRocks + ClickHouse sinks (E1)">
    <!-- dbup-mysql drives StarRocks (MySQL wire on :9030) via MySqlConnector, not Oracle MySql.Data. -->
    <PackageVersion Include="dbup-core" Version="6.1.1" />
    <PackageVersion Include="dbup-mysql" Version="6.1.0" />
    <PackageVersion Include="MySqlConnector" Version="2.6.1" />
    <!-- ClickHouse has no DbUp provider; a purpose-built DbUp-pattern runner talks to it over ADO. -->
    <PackageVersion Include="ClickHouse.Client" Version="7.14.0" />
  </ItemGroup>

  <ItemGroup Label="Microsoft.Extensions hosting + DI + logging + config (net10)">
    <PackageVersion Include="Microsoft.Extensions.DependencyInjection" Version="10.0.7" />
    <PackageVersion Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.7" />
    <PackageVersion Include="Microsoft.Extensions.Hosting" Version="10.0.0" />
    <PackageVersion Include="Microsoft.Extensions.Hosting.Abstractions" Version="10.0.0" />
    <PackageVersion Include="Microsoft.Extensions.Configuration" Version="10.0.7" />
    <PackageVersion Include="Microsoft.Extensions.Configuration.Abstractions" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Extensions.Configuration.EnvironmentVariables" Version="10.0.7" />
    <PackageVersion Include="Microsoft.Extensions.Logging" Version="10.0.0" />
    <PackageVersion Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.7" />
    <PackageVersion Include="Microsoft.Extensions.Logging.Console" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup Label="Test stack (E12: xUnit + FluentAssertions + NetArchTest + Testcontainers)">
    <PackageVersion Include="xunit" Version="2.9.2" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="2.8.2" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.5.1" />
    <PackageVersion Include="FluentAssertions" Version="6.12.2" />
    <PackageVersion Include="coverlet.collector" Version="10.0.0" />
    <PackageVersion Include="NetArchTest.Rules" Version="1.3.2" />
    <!-- All Testcontainers packages must share one core version (a module built against a different
         core throws MissingMethodException at runtime). Sink gates add a ClickHouse module + the base
         package for the StarRocks allin1 image (no dedicated Testcontainers.StarRocks module). -->
    <PackageVersion Include="Testcontainers" Version="4.13.0" />
    <PackageVersion Include="Testcontainers.MsSql" Version="4.13.0" />
    <PackageVersion Include="Testcontainers.ClickHouse" Version="4.13.0" />
  </ItemGroup>

</Project>
